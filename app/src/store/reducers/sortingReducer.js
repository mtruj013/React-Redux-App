export const initialState = {
    result: '',
    isLoading: false,
    error: '' 
}

export const sortingReducer = (state = initialState, action) => {
    switch (action.type){
        default:
            return state;
    }
}